[package]
name = "demo_api"
version = "0.1.0"
edition = "2021"
description = "Rust HTTP verification API for the Medicine Tracker frontend demo"

[workspace]

[dependencies]
mongodb = { version = "=3.7.0", features = ["sync"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
